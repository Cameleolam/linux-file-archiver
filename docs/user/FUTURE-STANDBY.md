# Memo on diverse subjects to explore/remind
## Standby subjects

Learn Debian packaging
Edge case discovery (Robustness ADR)
File locking strategy (Robustness ADR)

## Future

- `dry-run` optional flag for the cli usage (might be to implement actually, more useful than user)
- `user` optional flag for the cli usage
- follow symlinks till they leave home. if they do : skip/log them